# 系统详细使用说明文档 (User Manual)

## 1. 系统概述 (System Overview)
面试管理系统是一个用于帮助企业或招聘团队安排、跟踪和评估候选人面试过程的应用程序。它主要包含两大功能模块：面试预约管理和面试评价管理。通过该系统，用户可以方便地创建和管理面试日程，并记录面试后的评价反馈。

## 2. 系统访问与主界面 (System Access & Main Interface)

### 2.1. 访问系统
根据部署方式，通过浏览器访问系统前端的 `index.html` 页面。
*   如果通过 Web 服务器部署，访问如 `http://your_domain_or_ip/`。
*   如果直接本地打开，访问 `index.html` 的文件路径。

### 2.2. 主界面
系统主界面采用 Layui 框架构建，布局清晰简洁。主要包含以下几个部分：
*   **顶部标题栏 (Header):** 显示系统名称“面试管理系统”和一些快捷操作（如用户信息、退出等）。
*   **左侧导航菜单 (Left Navigation Menu):**
    *   **面试预约管理 (Interview Appointment Management):** 点击进入面试预约相关操作界面。
    *   **面试评价管理 (Interview Evaluation Management):** 点击进入面试评价相关操作界面。
*   **主内容区 (Main Content Area):** 一个选项卡式（Tab）区域，用于加载和显示各个功能模块的具体内容页面。默认显示“首页”或欢迎信息。

*(主界面截图示意：由于无法生成实际截图，请设想一个典型的 Layui 后台管理界面，左侧为树形菜单，右侧为内容显示区域。)*

## 3. 面试预约管理 (Interview Appointment Management)
此模块用于管理所有的面试预约信息。

### 3.1. 查看预约 (Viewing Appointments)
1.  在左侧导航菜单中，点击“面试预约管理”。
2.  主内容区将加载并显示面试预约列表。该列表以表格形式展示，支持分页。
3.  主要列信息包括：
    *   **候选人姓名 (Candidate Name)**
    *   **面试时间 (Interview Time)**
    *   **面试官 (Interviewer)**
    *   **状态 (Status):** 例如“已安排”、“已完成”、“已取消”。
    *   **备注 (Notes)**
    *   **操作 (Actions):** 包括“编辑”和“删除”按钮。

### 3.2. 搜索预约 (Searching Appointments)
1.  在列表上方的搜索区域，找到“候选人姓名”输入框。
2.  输入您想要查找的候选人姓名（支持模糊搜索）。
3.  点击“搜索”按钮。
4.  列表将刷新并显示符合条件的预约记录。

### 3.3. 新增预约 (Adding an Appointment)
1.  点击列表上方的“新增预约”按钮。
2.  系统将弹出一个表单窗口，需要填写以下信息：
    *   **候选人姓名:** 必填。
    *   **面试时间:** 必填，通过日期时间选择器选择。
    *   **面试官:** 必填。
    *   **状态:** 下拉选择，默认为“已安排”（可选：“已安排”、“已完成”、“已取消”）。
    *   **备注:** 可选，填写额外信息。
3.  填写完毕后，点击表单底部的“提交”按钮。
4.  成功添加后，弹窗关闭，列表将自动刷新显示新记录。系统会提示“新增成功”。

*(新增预约表单截图示意：一个包含上述字段的 Layui 风格表单弹窗。)*

### 3.4. 编辑预约 (Editing an Appointment)
1.  在预约列表中，找到您想要修改的预约记录。
2.  点击该行记录对应的“操作”列下的“编辑”按钮。
3.  系统将弹出一个表单窗口，其中已预先填入了该预约的现有信息。
4.  根据需要修改表单中的信息。
5.  点击表单底部的“提交”按钮。
6.  成功更新后，弹窗关闭，列表将自动刷新。系统会提示“更新成功”。

### 3.5. 删除预约 (Deleting an Appointment)
1.  在预约列表中，找到您想要删除的预约记录。
2.  点击该行记录对应的“操作”列下的“删除”按钮。
3.  系统将弹出一个确认对话框，提示“确定要删除这条预约吗？”。
4.  点击“确定”按钮继续删除，点击“取消”则不执行操作。
5.  确认删除后，该记录将从列表中移除。系统会提示“删除成功”。

## 4. 面试评价管理 (Interview Evaluation Management)
此模块用于管理面试结束后的评价信息。

### 4.1. 查看评价 (Viewing Evaluations)
1.  在左侧导航菜单中，点击“面试评价管理”。
2.  主内容区将加载并显示面试评价列表，支持分页。
3.  主要列信息包括：
    *   **关联预约ID (Appointment ID)**
    *   **评价内容 (Interviewer Comments)**
    *   **评分 (Rating):** 例如“5星”。
    *   **评价结果 (Result):** 例如“录用”、“淘汰”、“待定”。
    *   **评价时间 (Evaluation Time)**
    *   **操作 (Actions):** 包括“编辑”和“删除”按钮。

### 4.2. 搜索评价 (Searching Evaluations)
1.  在列表上方的搜索区域，找到“关联预约ID”输入框。
2.  输入您想要查找评价对应的预约ID。
3.  点击“搜索”按钮。
4.  列表将刷新并显示符合条件的评价记录。

### 4.3. 新增评价 (Adding an Evaluation)
1.  点击列表上方的“新增评价”按钮。
2.  系统将弹出一个表单窗口，需要填写以下信息：
    *   **关联预约ID:** 必填，从下拉列表中选择一个已存在的面试预约。下拉列表会显示预约的“ID - 候选人姓名 (面试日期)”。
    *   **评价内容:** 必填，面试官对候选人的评价。
    *   **评分:** 必填，从下拉列表选择评分 (1星到5星)。
    *   **评价结果:** 必填，从下拉列表选择评价结果 (例如：“待定”、“录用”、“淘汰”)。
3.  填写完毕后，点击表单底部的“提交”按钮。
4.  成功添加后，弹窗关闭，列表将自动刷新。系统会提示“新增成功”。

*(新增评价表单截图示意：一个包含上述字段的 Layui 风格表单弹窗，其中“关联预约ID”为下拉选择框。)*

### 4.4. 编辑评价 (Editing an Evaluation)
1.  在评价列表中，找到您想要修改的评价记录。
2.  点击该行记录对应的“操作”列下的“编辑”按钮。
3.  系统将弹出一个表单窗口，其中已预先填入了该评价的现有信息。
4.  根据需要修改表单中的信息。（通常，关联预约ID在编辑时可能不允许修改，或需谨慎操作。）
5.  点击表单底部的“提交”按钮。
6.  成功更新后，弹窗关闭，列表将自动刷新。系统会提示“更新成功”。

### 4.5. 删除评价 (Deleting an Evaluation)
1.  在评价列表中，找到您想要删除的评价记录。
2.  点击该行记录对应的“操作”列下的“删除”按钮。
3.  系统将弹出一个确认对话框，提示“确定要删除这条评价吗？”。
4.  点击“确定”按钮继续删除。
5.  确认删除后，该记录将从列表中移除。系统会提示“删除成功”。

---
用户手册结束。
